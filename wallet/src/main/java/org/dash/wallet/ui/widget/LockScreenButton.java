package org.dash.wallet.ui.widget;

import android.content.Context;
import android.util.AttributeSet;
import androidx.appcompat.widget.AppCompatButton;

public class LockScreenButton extends AppCompatButton {
    public LockScreenButton(Context context) {
        super(context);
    }

    public LockScreenButton(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public LockScreenButton(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }
}
